#include <stdio.h>

int main() {
    int nombre;
    printf("entre un nombre");
    scanf("%d",&nombre);
    if(nombre%2==0) {
        printf("nombre et pir");
        
    }
    else printf("nomber impir");
    
    return 0;
}   